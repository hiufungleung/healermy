# =============================================================================
# HealerMy Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values
# For Docker deployment: cp .env.example .env
# =============================================================================

# =============================================================================
# REQUIRED: Application URLs
# =============================================================================

# Your public application URL (HTTPS in production)
# Examples:
#   - Production: https://healermy.yourdomain.com
#   - Staging: https://staging.healermy.yourdomain.com
#   - Local: http://localhost:3000
BASE_URL=https://healermy.yourdomain.com

# FHIR server endpoint URL (server-side only for better security)
# Examples:
FHIR_SERVER_URL=https://gw.interop.community/sandbox/data

# =============================================================================
# REQUIRED: SMART on FHIR Credentials
# =============================================================================

# Client ID from your FHIR app registration
CLIENT_ID=your-client-id-here

# Client Secret from your FHIR app registration
# Note: Public apps may not have a secret (leave empty if not applicable)
CLIENT_SECRET=your-client-secret-here

# Session encryption secret (generate with: openssl rand -hex 32)
SESSION_SECRET=your-generated-session-secret-here

# Session encryption salt (generate with: openssl rand -hex 32)
SESSION_SALT=your-generated-session-salt-here

# =============================================================================
# OPTIONAL: Application Configuration (defaults shown)
# =============================================================================

# Access type: "online" (session-only) or "offline" (with refresh token)
ACCESS_TYPE=offline

# Session expiry: s(seconds), m(minutes), h(hours), d(days), y(years)
SESSION_EXPIRY=90d

# =============================================================================
# OPTIONAL: FHIR Permission Scopes (defaults provided in Dockerfile)
# =============================================================================
# Uncomment and customize if you need different scopes

# Patient scopes (read-only access to own data)
# PATIENT_SCOPE_OFFLINE="launch/encounter launch/patient openid profile offline_access launch fhirUser user/*.* patient/*.*"
# PATIENT_SCOPE_ONLINE="launch/encounter launch/patient openid profile online_access launch fhirUser user/*.* patient/*.*"

# Provider scopes (full CRUD access)
# PROVIDER_SCOPE_OFFLINE="launch/encounter launch/patient openid profile offline_access launch fhirUser user/*.* patient/*.*"
# PROVIDER_SCOPE_ONLINE="launch/encounter launch/patient openid profile online_access launch fhirUser user/*.* patient/*.*"

# =============================================================================
# OPTIONAL: Cookie Configuration (defaults provided in codebase)
# =============================================================================
# Uncomment only if you need to customize cookie names

# SESSION_COOKIE_NAME=healermy_session  # Default: healermy_session
# TOKEN_COOKIE_NAME=healermy_tokens      # Default: healermy_tokens

# =============================================================================
# DOCKER-SPECIFIC (Usually don't need to change)
# =============================================================================

# Port the application listens on inside the container
# PORT=3000

# Network interface to bind to (0.0.0.0 = all interfaces)
# HOSTNAME=0.0.0.0

# Node environment
# NODE_ENV=production
